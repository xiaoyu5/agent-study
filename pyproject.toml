[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "education-agent"
version = "0.1.0"
description = "A step-by-step educational agent learning project."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
education-agent = "education_agent.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
